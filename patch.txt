--- app/src/main/java/com/example/ui/WebViewScreen.kt
+++ app/src/main/java/com/example/ui/WebViewScreen.kt
@@ -152,22 +152,7 @@
         }
     }
 
-    androidx.compose.foundation.layout.Box(
-        modifier = Modifier
-            .fillMaxSize()
-            .pointerInput(Unit) {
-                awaitPointerEventScope {
-                    while (true) {
-                        val event = awaitPointerEvent(PointerEventPass.Initial)
-                        val isDownEdge = event.changes.any { it.pressed && !it.previousPressed }
-                        if (isDownEdge && !isLocked && !showControlPanel) {
-                            showSettingsFab = true
-                            fabTimer++
-                        }
-                    }
-                }
-            }
-    ) {
+    androidx.compose.foundation.layout.Box(modifier = Modifier.fillMaxSize()) {
         AndroidView(
             factory = { context ->
                 WebView(context).apply {
@@ -183,6 +168,14 @@
                     settings.allowContentAccess = true
                     settings.setSupportMultipleWindows(true)
                     
+                    setOnTouchListener { _, event ->
+                        if (event.action == android.view.MotionEvent.ACTION_DOWN && !isLocked && !showControlPanel) {
+                            showSettingsFab = true
+                            fabTimer++
+                        }
+                        false
+                    }
+                    
                     if (android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.KITKAT) {
                         WebView.setWebContentsDebuggingEnabled(true)
                     }
